<% content_for :title, '编辑用户' %>

<div class="page-header">
  <div>
    <h1><i class="bi bi-person-gear"></i> 编辑用户</h1>
  </div>
  <div>
    <%= link_to '返回列表', admin_users_path, class: 'btn btn-outline-secondary' %>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <%= render 'form', user: @user %>
  </div>
</div>
