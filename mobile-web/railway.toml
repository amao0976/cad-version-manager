# Railway 前端部署配置
# 生产环境变量请通过 Railway Dashboard 的 Variables 面板设置 VITE_API_BASE_URL
# 不要在此文件中写空值，否则会覆盖 .env.production
[build]
  builder = "nixpacks"
  buildCommand = "npm install && npm run build"
  startCommand = "npx serve dist -l $PORT"
